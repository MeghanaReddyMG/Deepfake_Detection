# Ultra-minimal requirements for Docker deployment (target: <1GB final image)
flask==2.3.3
python-dotenv==1.0.0

# Image processing and analysis (use versions with better wheel support)
Pillow==10.0.0
numpy==1.24.3
scipy==1.11.1

# File handling
werkzeug==2.3.7

# Production server
gunicorn==21.2.0

# Add explicit wheel support
wheel